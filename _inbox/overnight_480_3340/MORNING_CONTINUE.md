# Subah continue — auto mode checklist

Owner bolega **continue** → yeh order:

1. `gh pr list --draft` — overnight DRAFT PRs (#57–#63+) review
2. Conflicts resolve; **merge good DRAFT overnight PRs to `main`** (Hostinger Git auto-deploy)
3. Skip / close duplicate overlapping overnight PRs
4. Live smoke: homepage, signup, demo, checkout, admin_website + key admin 302
5. Remaining OPEN rows in `QUEUE.md` → finish or new cloud relay
6. Owner-only left as BLOCKED (keys, Axis live, 3315–3339)
7. Update PHOTO_NOTES + short chat report: kitne points DONE / SKIP / blocked

**Do not** invent secrets. **Do** prefer Git deploy over FTP.
